namespace Bonsai.ML.Torch.NeuralNets
{
    /// <summary>
    /// Represents a loss function that computes the loss value for a given input and target tensor.
    /// </summary>
    public enum Loss
    {
        /// <summary>
        /// Computes the negative log likelihood loss.
        /// </summary>
        NLLLoss,
    }
}